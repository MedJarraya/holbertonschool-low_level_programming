#include "holberton.h"

/**
 * _memcpy - str
 *
 *@dest: int
 *@src: int
 *@n: int
 *Return: 0
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	int a;

	a = 0;
	while (a < (int)n)
	{
		dest[a] = src[a];
		a++;
	}
	return (dest);
}
