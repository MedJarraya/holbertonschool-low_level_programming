#include "holberton.h"
/**
*print_chessboard - print
*@a: var
*
*
*/
void print_chessboard(char (*a)[8])
{
	int z, w;

	for (z = 0; z < 8; ++z)
	{
		for (w = 0; w < 8; ++w)
		{
			_putchar(a[z][w]);
		}
		_putchar('\n');
	}
}
